
spring:
  security:
    user:
      name: admin
      password: "{noop}admin123"
  servlet:
    multipart:
      enabled: true
  datasource:
    url: jdbc:postgresql://109.198.190.115:32322/MakarovM
    username: student
    password: "5432"
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect